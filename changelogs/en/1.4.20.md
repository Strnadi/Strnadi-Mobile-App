- Migrated iOS app lifecycle to a scene-based setup (`UISceneManager`/`FlutterSceneDelegate`) for better compatibility with newer iOS runtime behavior.
- Refactored authentication and registration API calls to use centralized `AuthController` and `UserController` flows instead of direct HTTP calls.
- Improved login, sign-up, and password reset handling with stronger response parsing, user ID retrieval checks, and clearer error logging across email, Google, and Apple flows.
