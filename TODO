add the database work when offline

trim tha audio and make the data right 
(each metadato for each part - a list of not only split time but the whole metadata shit)

fix the audio trim to stop the recording 

Add the scrollable elemnt to the login

Email the testflight link to do the external testing

remake the spectogram recorder (paper notebook) 

offline database model to store the data locally till internet conn. available

change the jwt token to be in the header as bearer
